# Environment Variables Template
# Copy this file to .env.local (for local development) or .env (for production)
# ⚠️ NEVER commit actual .env files to version control!

# ============================================
# REQUIRED VARIABLES
# ============================================

# Database
# PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/internal_app?schema=public"

# NextAuth Configuration
# Base URL of your application
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXTAUTH_URL="http://localhost:3000"

# NextAuth Secret Key
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-key-change-in-production"

# ============================================
# SECURITY (Required for Production)
# ============================================

# Encryption Key
# Required for encrypting sensitive client data (passwords, API keys, etc.)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Or: openssl rand -hex 32
# ⚠️ MUST be 64 characters (32 bytes hex encoded)
# ⚠️ NEVER share this key or commit it to version control
ENCRYPTION_KEY="your-64-character-hex-encryption-key-here"

# ============================================
# OPTIONAL: S3-Compatible Storage
# Leave unset to use local file storage
# ============================================

# For AWS S3:
# S3_BUCKET_NAME="your-bucket-name"
# S3_ACCESS_KEY_ID="your-access-key-id"
# S3_SECRET_ACCESS_KEY="your-secret-access-key"
# S3_REGION="us-east-1"

# For Cloudflare R2 (S3-compatible):
# S3_BUCKET_NAME="your-bucket-name"
# S3_ACCESS_KEY_ID="your-r2-access-key-id"
# S3_SECRET_ACCESS_KEY="your-r2-secret-access-key"
# S3_REGION="auto"
# S3_ENDPOINT="https://your-account-id.r2.cloudflarestorage.com"

# ============================================
# OPTIONAL: WhatsApp Notifications (Twilio)
# Leave unset to disable WhatsApp notifications
# ============================================

# WhatsApp Provider
# Options: "twilio", "webhook", or "none" (default: "none")
WHATSAPP_PROVIDER="none"

# Twilio Configuration (Required if WHATSAPP_PROVIDER="twilio")
TWILIO_ACCOUNT_SID="your-twilio-account-sid"
TWILIO_AUTH_TOKEN="your-twilio-auth-token"
TWILIO_WHATSAPP_FROM="+1234567890"

# Twilio WhatsApp Template (Recommended for Production)
# Get Template SID from Twilio Console → Messaging → Content Templates
TWILIO_WHATSAPP_TEMPLATE_SID="HXxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
TWILIO_USE_TEMPLATE="true"

# Webhook Configuration (Alternative to Twilio)
# Use this if WHATSAPP_PROVIDER="webhook"
# WHATSAPP_WEBHOOK_URL="https://your-webhook-endpoint.com/send-whatsapp"

# ============================================
# OPTIONAL: Socket.IO Configuration
# ============================================

# Socket.IO URL (for client-side connection)
# Leave empty to auto-detect from current domain
# NEXT_PUBLIC_SOCKET_URL=""

# ============================================
# NOTES
# ============================================
# - NODE_ENV is automatically set by Next.js (development/production)
# - PORT and HOSTNAME can be set for custom server configuration
# - See ENVIRONMENT_VARIABLES.md for detailed documentation
